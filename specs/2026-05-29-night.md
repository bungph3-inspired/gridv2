# Night spec — 2026-05-29

> Second real night assignment. First cycle (2026-05-28 → 05-29) shipped #46
> implementation but PR creation was blocked by missing fleet-PAT scopes; PAT
> rotated today with full repo write, branch `agent/issue-46` cleared via
> manual PR #47.
>
> Tonight is intentionally minimal — one tiny removal-only task so we get a
> clean canary signal that the loop end-to-end is healthy after today's
> workspace reorg + PAT rotation.

## Goal

Finish the cleanup tracked as out-of-scope on issue #45: strip the last
`ginjury` reference from `src/main.js` and delete the stale `src/main.js.bak`
backup file. Both are leftovers from PR23 (which removed `BS_GAME_META` and
the `ginjury` class entirely).

## Out of scope

- Do not touch any other dead-class references that may exist elsewhere
  in `src/` — if you find them, file a follow-up issue rather than
  expanding this PR's diff.
- Do not modify `src/state.js` — state-shape changes need a human review
  pass.
- Do not change colors, palettes, or any of the home-view skin tokens.

## Tasks

### Task 1 — Strip the last `ginjury` reference from `src/main.js`

- **Context:** PR23 (commit `00c2dbe`) removed the `BS_GAME_META` const +
  `getGameMeta` function + the optional meta-row rendering in
  `buildGameBlock`. One comment line in `src/main.js` still references
  `ginjury` as part of a list of rows skipped on props-only leagues —
  but `ginjury` no longer exists, so the mention is stale.
- **Acceptance:**
  - The line in `src/main.js` that currently reads
    `// matchup context, so we skip ginfo / ginjury / .trow-g rows on this view.`
    is updated to drop the `ginjury` mention. The corrected line reads
    `// matchup context, so we skip ginfo / .trow-g rows on this view.`
  - `grep -nE '\bginjury\b' src/main.js` returns no matches
  - `npx vite build` (from repo root) completes with no new warnings or
    errors compared to `main` HEAD before this PR
  - Diff is a single-line comment edit; no other lines touched in
    `src/main.js`
- **Touch:** `src/main.js`

### Task 2 — Delete `src/main.js.bak`

- **Context:** `src/main.js.bak` is the pre-PR23 backup of `src/main.js`,
  containing the dead `BS_GAME_META` / `getGameMeta` / meta-row block that
  PR23 already removed from the live file. It's 1425 lines of stale code
  that confuses grep-the-source workflows and adds noise to any audit of
  dead references.
- **Acceptance:**
  - `src/main.js.bak` is deleted (single-file delete; no rename, no
    relocation)
  - `git status` after the delete shows exactly one deletion and no
    other changes
  - Nothing else in the repo references `src/main.js.bak` — verify
    with `grep -RE 'main\.js\.bak' .` returning no matches (other than
    inside `.git/`)
  - `npx vite build` (from repo root) completes with no new warnings or
    errors compared to `main` HEAD before this PR
- **Touch:** `src/main.js.bak` (delete)

## Notes for the fleet

- Both tasks are deliberately tiny and removal-only — the canary signal
  is "did the Architect → Coder → Reviewer → merger loop complete after
  today's PAT rotation and workspace reorg", not "did we ship important
  code."
- The fleet PAT was rotated today with full Repository write scope
  (code + issues + pull requests + workflows). If PR creation or
  label/comment writes fail tonight, something else regressed — file
  a diagnostic issue rather than rolling back.
- Frontend builds via Cloudflare Pages on every `main` merge — no VPS
  deploy step needed. Just make sure `npx vite build` is clean before
  opening each PR.
- The repo's `node --check` works on `.js` files; use it as a quick
  syntax sanity check before committing the edit to `src/main.js`.
- If a task fails verification, file an issue comment with the
  diagnostic rather than rolling back — humans triage in the morning.
