  .mob-prop-btn .mob-prop-line { @apply font-display font-semibold text-[12px]; }
  .mob-prop-btn .mob-prop-vig  { @apply text-[10px] text-bet-text-sm; }
  .mob-prop-btn.sel .mob-prop-vig { @apply text-[#aee]; }

  /* ─── AGENT VIEW (bookie-side portal) ─────────────────────────────────────
     Toggled by header button. Replaces #main-layout when active. Mirrors
     LC797's agent portal layout. Live scoreboard intentionally omitted.
     See AGENT_RECON.md for the source-of-truth recon. */
  .agent-view {
    @apply flex-1 overflow-y-auto bg-bet-bg;
  }
  .ag-shell        { @apply flex flex-col h-full; }
  .ag-topbar       { @apply bg-bet-darker text-white px-4 py-2 flex items-center gap-4 flex-shrink-0; }
  .ag-id           { @apply font-display text-lg font-bold tracking-wider; }
  .ag-bal          { @apply font-display text-sm text-bet-text-xs; }
  .ag-bal-val      { @apply text-white font-bold; }
  .ag-crumb        { @apply font-body text-xs text-bet-text-xs ml-auto; }
  .ag-crumb-sub    { @apply font-body text-sm text-bet-text; }
  .ag-crumb-sub b  { @apply text-bet-darker; }
  .ag-subhdr       { @apply bg-bet-darker text-white px-4 py-2 flex items-center gap-3 flex-shrink-0; }
  .ag-subhdr .ag-crumb-sub { @apply text-bet-text-xs; }
  .ag-subhdr .ag-crumb-sub b { @apply text-white; }
  .ag-subhdr-actions { @apply ml-auto flex gap-2; }
  .ag-back         { @apply bg-transparent border border-white/40 text-white text-xs px-2 py-1 rounded cursor-pointer hover:bg-white/10; }
  .ag-action       { @apply bg-bet-panel border border-bet-border text-bet-text text-xs px-3 py-1 rounded cursor-pointer hover:bg-white; }

  .ag-body         { @apply flex flex-1 overflow-hidden; }
  .ag-main         { @apply flex-1 overflow-y-auto p-4 flex flex-col gap-3 min-w-0; }
  .ag-rail         { @apply w-[280px] border-l border-bet-border bg-bet-panel flex flex-col flex-shrink-0; }
  .ag-rail-tabs    { @apply flex border-b border-bet-border; }
  .ag-rail-tab     { @apply flex-1 text-center font-display text-xs uppercase tracking-wider py-2 cursor-pointer text-bet-text-sm; }
  .ag-rail-tab.active { @apply text-bet-darker border-b-2 border-bet-accent; }
  .ag-rail-body    { @apply p-3 flex flex-col gap-2 overflow-y-auto; }
  .ag-rail-search  { @apply w-full px-2 py-1 text-xs border border-bet-border rounded bg-white; }
  .ag-hier-row     { @apply flex justify-between text-sm px-2 py-1 hover:bg-bet-alt rounded cursor-pointer; }
  .ag-hier-name    { @apply font-display font-semibold text-bet-darker; }
  .ag-hier-cnt     { @apply text-bet-text-xs text-xs; }

  .ag-search       { @apply flex gap-2 items-center; }
  .ag-search-inp   { @apply flex-1 px-3 py-2 border border-bet-border rounded bg-white text-sm; }
  .ag-addnew       { @apply bg-bet-green text-white font-display font-bold text-xs uppercase tracking-wider px-4 py-2 rounded cursor-pointer hover:opacity-90; }

  .ag-kpi          { @apply grid grid-cols-4 gap-0 bg-[#cfe1e6] rounded overflow-hidden border border-bet-border; }
  .ag-kpi-cell     { @apply px-4 py-3 text-center border-r border-bet-border last:border-r-0; }
  .ag-kpi-cell label { @apply block font-display text-[10px] uppercase tracking-wider text-bet-text-sm; }
  .ag-kpi-val      { @apply font-display text-lg font-bold text-bet-darker tabular-nums; }

  .ag-cs           { @apply bg-bet-red text-white text-center font-display font-bold uppercase tracking-wider py-2 rounded text-sm cursor-pointer hover:opacity-90; }
  .ag-feedback     { @apply bg-bet-darker text-white text-center font-display font-bold uppercase tracking-wider py-2 rounded text-sm cursor-pointer hover:opacity-90; }

  .ag-grid         { @apply grid grid-cols-3 gap-2; }
  .ag-tile         { @apply bg-white border border-bet-border rounded p-4 flex flex-col items-center gap-2 cursor-pointer hover:shadow-card-hover transition-shadow; }
  .ag-tile-icon    { @apply text-2xl; }
  .ag-tile-label   { @apply font-display text-xs uppercase tracking-wider text-bet-text font-semibold; }
  .ag-tile.tile-blue   .ag-tile-icon { @apply text-blue-600; }
  .ag-tile.tile-purple .ag-tile-icon { @apply text-purple-600; }
  .ag-tile.tile-amber  .ag-tile-icon { @apply text-amber-500; }
  .ag-tile.tile-red    .ag-tile-icon { @apply text-bet-red; }
  .ag-tile.tile-green  .ag-tile-icon { @apply text-bet-green; }
  .ag-tile.tile-teal   .ag-tile-icon { @apply text-teal-600; }
  .ag-tile.tile-navy   .ag-tile-icon { @apply text-bet-darker; }
  .ag-tile.tile-orange .ag-tile-icon { @apply text-orange-600; }

  .ag-widget       { @apply bg-white border border-bet-border rounded; }
  .ag-widget summary { @apply bg-bet-darker text-white font-display text-xs uppercase tracking-wider px-3 py-2 cursor-pointer list-none; }
  .ag-widget summary::-webkit-details-marker { display: none; }
  .ag-widget summary::after { content: " ▾"; @apply float-right; }
  .ag-widget[open] summary::after { content: " ▴"; }
  .ag-wrow         { @apply grid gap-2 px-3 py-1.5 text-xs border-t border-bet-alt; grid-template-columns: 80px 60px 1fr 80px; }
  .ag-wmono        { @apply font-mono text-bet-text-sm; }
  .ag-wamt         { @apply text-right tabular-nums font-semibold; }
  .ag-wdate        { @apply text-right text-bet-text-xs tabular-nums; }
  .ag-empty        { @apply text-bet-text-xs italic text-center py-3 text-xs; }

  /* Management table */
  .ag-table-wrap   { @apply flex-1 overflow-auto bg-white; }
  .ag-table        { @apply w-full text-sm border-collapse; }
  .ag-table thead th { @apply bg-bet-panel text-bet-darker font-display text-[11px] uppercase tracking-wider px-2 py-2 border-b-2 border-bet-border text-left sticky top-0 z-10; }
  .ag-table tbody td { @apply px-2 py-1.5 border-b border-bet-alt; }
  .ag-table tbody tr:nth-child(even) { @apply bg-bet-alt/40; }
  .ag-table tbody tr:hover { @apply bg-bet-accent/10; }
  .ag-table tfoot th, .ag-table tfoot td { @apply px-2 py-2 font-display font-bold text-bet-darker bg-bet-panel border-t-2 border-bet-border; }
  .ag-table .num   { @apply text-right tabular-nums; }
  .ag-pid          { @apply font-display; }
  .ag-mono         { @apply font-mono text-bet-text-xs; }
  .ag-link         { @apply underline decoration-dotted underline-offset-2 cursor-pointer text-bet-darker hover:text-bet-accent; }
  .ag-dim          { @apply text-bet-text-xs; }
  .ag-pos          { @apply text-bet-green; }
  .ag-neg          { @apply text-bet-red; }

  .ag-pill         { @apply inline-block min-w-[36px] text-center font-display text-[10px] uppercase tracking-wider px-2 py-0.5 rounded-full border; }
  .ag-pill.on      { @apply bg-bet-green text-white border-bet-green; }
  .ag-pill.off     { @apply bg-white text-bet-text-xs border-bet-border; }
  .ag-pill-racing.on, .ag-pill-casino.on { @apply bg-amber-400 text-bet-darker border-amber-500; }

  /* Stub view for unimplemented tiles */
  .ag-stub         { @apply flex flex-col items-center justify-center gap-3 p-12 text-center; }
  .ag-stub-icon    { @apply text-5xl; }
  .ag-stub-title   { @apply font-display text-2xl font-bold text-bet-darker uppercase tracking-wider; }
  .ag-stub-msg     { @apply text-bet-text-sm text-sm max-w-md; }
}
