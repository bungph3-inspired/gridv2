## Session Log
- **2026-05-18 (cont.)** — **Agent Portal section added — recon, separate Vite entry, login splash, Customer Detail.** Built the bookie-side agent portal as a parallel deliverable to the existing player board. **(1) Recon**: walked LC797's `/partner/index.html#/ag-panel/main` agent SPA via Claude in Chrome (login as NUBI004), mapped the full sidebar IA (Dashboard / Figures / Wagers / Mailbox / Transactions / Accounts / Casino / Racebook / History / Schedule / Payments / Default Settings), captured the dashboard layout (4-cell KPI strip → red CS banner → 3-col 11-tile grid → Feedback banner → Recent Wagers/Logins/Changes accordions → right rail Scoreboard/Hierarchy/Settings), the Management table columns (ID / Credit Limit / Wager Limit / Parlay Max / Teaser Max / Casino / Racing / Settle / Last Wager / Last Login / Pending / Balance), the Customer Detail header pills + 11-tab editor (PERSONAL / LIMITS / LINESET / TRANSACTIONS / WAGER / HISTORY / COMMUNICATIONS / CHANGES / CONNECTIONS / CASINO / BACKBONE), the Weekly Figures filter+settings modal, the Agent Management sub-tabs (Settle Figures / Player Count / Agent Distribution / Weekly Figures), and the right-rail Settings (account-identifier picker + dashboard widget toggles). All written up in **`AGENT_RECON.md`** (251 lines) with IA tree, table columns, interaction patterns, color/spacing notes, and proposed mock-data shape. **Carve-out explicitly captured: live scoreboard is excluded from GridV2 by user direction.** **(2) Initial in-app overlay** (subsequently superseded — kept the implementation though, see step 3): wired a `👤 Agent` header button + `#agent-view` div in `index.html` that toggled major chrome on/off, with `initAgent()`/`toggleAgentView()` in a new `src/agent.js`. User pushed back: agents shouldn't share the player UI at all, they should have a separate login. **(3) Reverted to separate-entry architecture** mirroring LC797's `/partner/` subdir pattern. Removed the Agent button, `#agent-view`, and agent-mock script tag from `index.html`. Built **`agent.html`** as a new Vite entry (54 lines), wired via `vite.config.js` `rollupOptions.input.agent`. Added **`src/agent-main.js`** (61 lines) as the agent entry script — gates on `localStorage.bs_agent`, patches `window.AGENT_MOCK.id` to the typed login ID, hands off to `initAgent()` from the existing `src/agent.js`. **`public/agent_mock.js`** (now 168 lines after enrichment, see step 5) provides the dataset: 50 mock players (NUB400–NUB449) with full identity + limits + casino/racing flags + 0-5 sample wagers each. Tailwind `@source` directive updated to include `agent.html` so its classes get generated. **(4) Player-side login splash added too** (scope expansion from user mid-session): replaced the hardcoded `YM101` Account display with `id="acct-disp"` populated from `localStorage.bs_player`. Login splash overlay added to `index.html` (Login ID + Password + Sign In, footer link to agent.html). `init()` in `src/main.js` now gates on `localStorage.bs_player` — first visit shows splash, returning visits skip to the board. New `submitPlayerLogin()`/`logoutPlayer()`/`setAccountDisp()` functions in main.js exposed on window. Header `📋 My Bets` button now flanked by `⎋ Logout` button. **(5) Customer Detail subview added** (per user direction: "Personal + Limits + Wager real, rest stubs, display-only"). `agent.js` grew to 556 lines: added `'detail'` subview to the router, `DETAIL_TABS` array with `real: true/false` markers, 11-tab nav strip with icon + label + active/stub styling. **Real tabs**: PERSONAL renders a 2-column layout (left: Identity section [website / password / agent / bettor type / reputation] + Settings section [status badge / racebook+mailbox+main-casino pill toggles]; right: Demographics section [nickname / first / last / email / phone / referred-by / notes] + Location section [city / state]). LIMITS renders two stacked cards (Credit Limit, Other Limit) + a Limits Chart table with rows for Wager Limit / Parlays / Teasers / If Bets / Reverse Action across columns Setting / Selection / Amount / Max Teams / X-Limit / Max Payout. WAGER renders a filter bar (Ticket # / Wagers / Placed / Amount) + a wager-history table with sport / type / line / placed / risk / to-win / result (colored chip — amber Pending, green Win, red Loss) / signed Net column. **Stub tabs** (Lineset / Transactions / History / Communications / Changes / Connections / Casino / Backbone) render a centered icon + label + "see AGENT_RECON.md" message. SAVE button rendered disabled with `title="Read-only first pass"`. Player rows in Management table are now clickable (`.ag-prow` with data-pid). Mock data **enriched** in step 5 to add firstName/lastName/email/phone/city/state/bettorType/reputation/status/racebook/mailbox/mainCasino + 0-5 sample wagers per player. **Token reuse**: agent UI uses existing `@theme` tokens (`bet-darker`, `bet-brand`, `bet-bg`, `bet-panel`, `bet-accent`, `bet-green`, `bet-red`, `bet-text*`) — only one new visual surface (`#cfe1e6` KPI strip background) is hard-coded inline, deferred to a `--color-bet-kpi` token if reused. **CSS**: ~6.9KB of `@layer components` classes added across two sessions — `ag-shell`, `ag-topbar`, `ag-body`, `ag-main`, `ag-rail`, `ag-kpi*`, `ag-grid`, `ag-tile*` (8 color variants), `ag-widget`, `ag-wrow*`, `ag-table*`, `ag-pill*` (with yellow `.on` variant for racing/casino), `ag-cust-hdr` + `ag-cust-pill*` + `ag-cust-tabs` + `ag-cust-tab*` + `ag-cust-body`, `ag-personal*` + `ag-sect-hdr` + `ag-frow`/`ag-flabel`/`ag-fval`, `ag-status*`, `ag-limits*` + `ag-lim-card*` + `ag-lim-chart*` + `ag-lim-table*`, `ag-wager*` + `ag-wfilters` + `ag-wager-table` + `ag-wres*`, `login-splash` + `login-card` + `login-brand` + `login-form` + `login-btn` + `login-footer`, `ag-logout`, `ag-stub*`, `ag-back*`, `ag-search*`, `ag-addnew`, `ag-cs`, `ag-feedback`, `ag-action`. All used inside `@layer components` with `@apply`. **Files added**: `AGENT_RECON.md`, `agent.html`, `src/agent.js`, `src/agent-main.js`, `public/agent_mock.js`. **Files touched**: `index.html` (login splash, dynamic Account display, Logout button — Agent button + #agent-view added then reverted), `src/main.js` (login gate in `init()` + 3 new login functions + window exposures), `src/style.css` (~6.9KB of `@layer components` + `@source "../agent.html"`), `vite.config.js` (added `agent` input), `verify/check_integrity.cjs` (5 new canonical files added: src/agent.js, src/agent-main.js, public/agent_mock.js, agent.html, AGENT_RECON.md). **Verification**: `node verify/check_integrity.cjs` clean across all 28 canonical files; `npm run build` produces dist/index.html (18.8KB), dist/agent.html (2.5KB), dist/index_mobile.html (8.5KB), dist/assets/main-*.js (60KB), dist/assets/agent-*.js (17KB), dist/assets/api-*.js (20KB), dist/assets/utils-*.js (3KB), dist/assets/utils-*.css (160KB). Build output is bigger than ideal but acceptable for the agent feature surface. **Edit-tool truncation incidents this session**: zero. `safe-edit.sh` used for every growing edit across `index.html`, `src/main.js`, `src/style.css`, `vite.config.js`, `verify/check_integrity.cjs` — no incidents. Heredoc-via-`cat > file` used for the four new files; all clean. The safe-edit discipline from prior sessions paid for itself this session. **Open threads next session**: (k) Wire the remaining 8 Customer Detail tabs (Lineset / Transactions / History / Communications / Changes / Connections / Casino / Backbone); (l) Build out the placeholder dashboard tiles (Weekly Figures real report with the column-options modal from recon, global Pending Wagers across all 50 players, Cashier transactions, Mass Edit, Position, Bet Ticker, IP Checker, global Transactions, Mailbox); (m) Wire the right-rail Hierarchy/Settings tabs to be functional (currently the SETTINGS tab is static, HIERARCHY just shows the agent ID + count); (n) Consider a `verify_agent.cjs` jsdom suite covering: login splash gate, dashboard render with mock data, tile clicks routing to Management, player row click routing to Detail, tab switching across PERSONAL/LIMITS/WAGER + stub tabs, logout clearing localStorage; (o) Mobile agent build (currently agent.html is desktop-only — no mobile bounce); (p) Editability pass for Customer Detail (currently read-only — convert form-style display to editable inputs with SAVE writing back to localStorage-persisted mock data). **Private hosting note**: user expressed concern about Netlify making the build public. Discussed options: Cloudflare Pages + Cloudflare Access (free, recommended), Vercel + Vercel Authentication (free), Netlify Pro password protection ($19/mo), Tailscale Funnel (free, zero public exposure), VPS + nginx basic auth. No deployment changes made this session; the existing Netlify Drop workflow is documented in the Deployment section above and stays the default until John picks a private host.
- **2026-05-18** — **Project renamed BetSimV2 → GridV2 + cyan token rename + TODO.md housekeeping.**
